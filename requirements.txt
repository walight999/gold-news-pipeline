feedparser>=6.0.10
httpx[http2]>=0.27.0
rapidfuzz>=3.6.0
pyyaml>=6.0
gspread>=6.0.0
google-auth>=2.27.0
python-dateutil>=2.8.2
tenacity>=8.2.3
# For weekly_preview HTML scrape (FF JSON only ships this-week data;
# Cloudflare blocks plain httpx so curl_cffi with chrome impersonation
# is required for the calendar?week=next page).
curl-cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# XAU + DXY intraday for post-release reaction + daily-calendar snapshot.
yfinance>=0.2.40
# Thai translation for Latest News Update digest. Faithful translation
# only (no AI rewriting). Google Translate backend = free, no API key.
deep-translator>=1.11.0
# Claude Haiku for higher-quality Thai translation of finance jargon.
# Falls back to deep-translator (Google) when ANTHROPIC_API_KEY is unset.
anthropic>=0.30.0
# X/Twitter posting for the social_post mode (approved social_feed drafts).
tweepy>=4.14.0
